# Database Configuration
# Prisma database connection URL

# For local development with SQLite (default)
DATABASE_URL="file:./dev.db"

# For PostgreSQL (production recommended)
# DATABASE_URL="postgresql://user:password@localhost:5432/smart_session_planner?schema=public"

# For MySQL
# DATABASE_URL="mysql://user:password@localhost:3306/smart_session_planner"

# Server Configuration
# Next.js will run on port 3001 (configured in package.json scripts)
# Make sure this matches EXPO_PUBLIC_API_URL in the front-end .env
